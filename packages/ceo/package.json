{
  "name": "@a2b/ceo",
  "version": "0.1.0",
  "description": "CEO Agent — the trusted supervisor that governs all other agents in the A2B framework",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test src/**/*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@a2b/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
